/* web-dashboard/src/App.css - tambahkan */
.logo {
  width: 80px;
  height: auto;
  margin-bottom: 10px;
}
.tagline {
  font-size: 14px;
  opacity: 0.9;
  font-style: italic;
  margin-top: 5px;
}
.admin-info {
  font-size: 13px;
  opacity: 0.8;
  margin-top: 8px;
  border-top: 1px solid rgba(255,255,255,0.2);
  padding-top: 8px;
}
